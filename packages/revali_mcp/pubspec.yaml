name: revali_mcp
description: Stdio MCP server for Revali — routes, doctor, inspect, create.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  path: ^1.9.1

executables:
  revali_mcp:

dev_dependencies:
  test: ^1.25.15
