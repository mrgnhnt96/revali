name: manual_gen
publish_to: 'none'

environment:
    sdk: '>=3.2.6 <4.0.0'

dependencies:
    client_gen_models:
    client:
      path: ../../.revali/revali_client
    revali_client:
    web_socket_channel: ^3.0.2